<nav class="w-full bg-emerald-600 text-white shadow-md sticky top-0 z-50 mb-6">
  <div class="max-w-7xl mx-auto px-6 h-16 flex items-center justify-between">
    
    <!-- Left: Brand / Logo -->
    <div class="flex-shrink-0">
      <a href="/" class="text-2xl font-bold tracking-tight hover:text-emerald-50 transition-colors">
        Airbnb
      </a>
    </div>

    <!-- Center/Right: Navigation Links -->
    <div class="flex items-center gap-8">
      
      <% if (isLoggedIn) { %>
        <% if (user.userType != "host") { %>
        <!-- Main Nav Links -->
        <div class="hidden md:flex items-center space-x-1 text-sm font-medium">
          <a href="/home" class="px-3 py-2 rounded-lg hover:bg-emerald-500 transition-colors">Home</a>
          <a href="/store/bookings" class="px-3 py-2 rounded-lg hover:bg-emerald-500 transition-colors">Bookings</a>
          <a href="/favourites" class="px-3 py-2 rounded-lg hover:bg-emerald-500 transition-colors">Favourites</a>

        <% } else { %>
          

          <a href="/host/host-home-list" class="px-3 py-2 rounded-lg hover:bg-emerald-500 transition-colors">Host Homes</a>
          <a href="/host/add-home" class="px-3 py-2 rounded-lg hover:bg-emerald-500 transition-colors">Add Home</a>
        </div>
            <% } %>
      <% } %>


      <!-- Auth Actions -->
      <div class="flex items-center space-x-4 border-l border-emerald-500 pl-6 ml-2">
        <% if (!isLoggedIn) { %>
          <a href="/login" class="text-sm font-medium hover:text-emerald-100 transition-colors">
            Log in
          </a>
          <a href="/signup" class="bg-white text-emerald-700 text-sm font-semibold px-5 py-2 rounded-full shadow-sm hover:bg-gray-50 hover:shadow-md transition-all">
            Sign up
          </a>
        <% } else { %>
          <form action="/logout" method="POST" class="m-0">
            <button type="submit" class="bg-emerald-800 text-white text-sm font-medium px-5 py-2 rounded-full shadow-sm hover:bg-emerald-900 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-offset-emerald-600 focus:ring-white transition-all">
              Log out
            </button>
          </form>
        <% } %>
      </div>

    </div>
  </div>
</nav>